# Node
node_modules/
.pnpm-store/
dist/
build/
.next/
.cache/

# Env / secrets
.env
.env.*
*.env
*.env.*
# ...but the template is documentation, not a secret: it records which vars
# the project reads (PUBLIC_GA_ID, etc.) and must be committed.
!.env.example

# Editor / OS
.DS_Store
*.swp
*~
.vscode/
.idea/

# Cloudflare
.wrangler/

# Caches
.eslintcache

# Astro (generated content cache + types — never tracked)
.astro/
